import { notFound } from 'next/navigation'; import ProjectForm from '@/components/ProjectForm'; import { updateProject } from '@/lib/actions'; import { getFormOptions,getProjectBase } from '@/lib/data';
export default async function Page({params}:{params:Promise<{id:string}>}){ const {id}=await params; const [project,options]=await Promise.all([getProjectBase(id),getFormOptions()]); if(!project) notFound(); return <><div className="page-head"><div><h1>案件編集</h1><p className="muted">{project.companyName} / {project.name}</p></div></div><ProjectForm project={project} options={options} action={updateProject}/></> }
