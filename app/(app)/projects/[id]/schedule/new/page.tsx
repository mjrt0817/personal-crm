import SubmitButton from '@/components/SubmitButton'; import { notFound } from 'next/navigation'; import { createSchedule } from '@/lib/actions'; import { getProjectHeader } from '@/lib/data'; export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await getProjectHeader(id);if(!p)notFound();return <><div className="page-head"><div><h1>予定追加</h1><p className="muted">{p.companyName} / {p.name}</p></div></div><form action={createSchedule} className="form-card"><input type="hidden" name="project_id" value={id}/><input type="hidden" name="company_id" value={p.companyId}/><label className="field"><span>件名 *</span><input name="title" required/></label><div className="form-grid two"><label className="field"><span>種別</span><select name="schedule_type"><option value="visit">訪問</option><option value="online">オンライン</option><option value="phone">電話</option><option value="work">作業</option><option value="deadline">締切</option><option value="other">その他</option></select></label><label className="field"><span>場所</span><input name="location"/></label><label className="field"><span>開始 *</span><input type="datetime-local" name="start_at" required/></label><label className="field"><span>終了</span><input type="datetime-local" name="end_at"/></label></div><label className="field"><span>内容</span><textarea name="description" rows={4}/></label><div className="form-actions"><a className="button" href={`/projects/${id}`}>キャンセル</a><SubmitButton pendingLabel="追加中…">追加</SubmitButton></div></form></>}
