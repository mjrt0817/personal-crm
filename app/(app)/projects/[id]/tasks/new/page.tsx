import SubmitButton from '@/components/SubmitButton'; import { notFound } from 'next/navigation'; import { createTask } from '@/lib/actions'; import { getProjectHeader } from '@/lib/data'; export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await getProjectHeader(id);if(!p)notFound();return <><div className="page-head"><div><h1>タスク追加</h1><p className="muted">{p.companyName} / {p.name}</p></div></div><form action={createTask} className="form-card"><input type="hidden" name="project_id" value={id}/><input type="hidden" name="company_id" value={p.companyId}/><label className="field"><span>タスク名 *</span><input name="title" required/></label><div className="form-grid two"><label className="field"><span>状態</span><select name="status"><option value="todo">未着手</option><option value="doing">対応中</option><option value="waiting">待ち</option></select></label><label className="field"><span>優先度</span><select name="priority" defaultValue="medium"><option value="high">高</option><option value="medium">中</option><option value="low">低</option></select></label><label className="field"><span>期限</span><input type="datetime-local" name="due_at"/></label></div><label className="field"><span>内容</span><textarea name="description" rows={4}/></label><div className="form-actions"><a className="button" href={`/projects/${id}`}>キャンセル</a><SubmitButton pendingLabel="追加中…">追加</SubmitButton></div></form></>}
