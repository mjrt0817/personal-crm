import { createSchedule } from '@/lib/actions'; import { getProjects } from '@/lib/data';
export default async function Page(){const projects=await getProjects();return <><div className="page-head"><div><h1>予定追加</h1><p className="muted">案件に紐づけて予定を登録します。</p></div></div><form action={createSchedule} className="form-card"><div className="form-section"><label className="field"><span>件名 *</span><input name="title" required/></label><label className="field"><span>案件</span><select name="project_id"><option value="">案件なし</option>{projects.map(p=><option key={p.id} value={p.id}>{p.companyName} / {p.name}</option>)}</select></label><div className="form-grid two"><label className="field"><span>種別</span><select name="schedule_type"><option value="visit">訪問</option><option value="online">オンライン</option><option value="phone">電話</option><option value="work">作業</option><option value="deadline">締切</option><option value="other">その他</option></select></label><label className="field"><span>場所</span><input name="location"/></label><label className="field"><span>開始 *</span><input type="datetime-local" name="start_at" required/></label><label className="field"><span>終了</span><input type="datetime-local" name="end_at"/></label></div><label className="field"><span>内容</span><textarea name="description" rows={4}/></label></div><div className="form-actions"><a className="button" href="/schedule">キャンセル</a><button className="button primary">追加</button></div></form></>}
