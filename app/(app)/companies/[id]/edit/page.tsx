import { notFound } from 'next/navigation'; import CompanyForm from '@/components/CompanyForm'; import { updateCompany } from '@/lib/actions'; import { getCompany } from '@/lib/data'; export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params; const company=await getCompany(id); if(!company)notFound(); return <><div className="page-head"><div><h1>取引先編集</h1><p className="muted">{company.name}</p></div></div><CompanyForm company={company} action={updateCompany}/></>}
