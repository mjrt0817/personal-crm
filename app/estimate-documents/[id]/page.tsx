import Link from "next/link";
import { notFound } from "next/navigation";
import PrintEstimateButton from "@/components/PrintEstimateButton";
import { getEstimateDocumentData } from "@/lib/data";
function yen(v:number){return `¥${Math.round(v).toLocaleString("ja-JP")}`;} function date(v?:string){return v?v.replaceAll("-","/"):"—";}
export default async function EstimateDocumentPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const doc=await getEstimateDocumentData(id);if(!doc)notFound();const {estimate,issuer,customer}=doc;return <div className="invoice-document-shell"><div className="invoice-document-toolbar no-print"><Link className="button" href={`/estimates/${id}`}>← 見積管理へ</Link><div className="row-actions"><Link className="button" href={`/estimates/${id}/edit`}>編集</Link><PrintEstimateButton/></div></div><main className="invoice-paper estimate-paper"><header className="invoice-doc-header"><div><h1>御 見 積 書</h1><div className="invoice-doc-customer"><div className="invoice-customer-name">{customer.name||estimate.companyName||"取引先"} 御中</div>{customer.postalCode&&<div>〒{customer.postalCode}</div>}{customer.address&&<div>{customer.address}</div>}{estimate.contactName&&<div>{estimate.contactName} 様</div>}</div></div><div className="invoice-doc-meta"><div><span>見積番号</span><strong>{estimate.estimateNo}</strong></div><div><span>見積日</span><strong>{date(estimate.issueDate)}</strong></div><div><span>有効期限</span><strong>{date(estimate.validUntil)}</strong></div></div></header><section className="invoice-doc-parties"><div className="invoice-doc-message"><p>下記の通りお見積り申し上げます。</p><div className="invoice-total-box"><span>お見積金額</span><strong>{yen(estimate.totalAmount)}</strong></div></div><div className="invoice-issuer"><strong>{issuer.name||"発行者情報未設定"}</strong>{issuer.postalCode&&<div>〒{issuer.postalCode}</div>}{issuer.address&&<div>{issuer.address}</div>}{issuer.phone&&<div>TEL：{issuer.phone}</div>}{issuer.email&&<div>{issuer.email}</div>}{issuer.registrationNumber&&<div>登録番号：{issuer.registrationNumber}</div>}</div></section><table className="invoice-lines estimate-lines"><thead><tr><th>品目</th><th>数量</th><th>単位</th><th>単価</th><th>金額</th></tr></thead><tbody>{estimate.items.map((item,index)=><tr key={item.id??index}><td>{item.description}</td><td>{item.quantity}</td><td>{item.unit??""}</td><td>{yen(item.unitPrice)}</td><td>{yen(item.lineSubtotal)}</td></tr>)}</tbody></table><section className="invoice-totals"><div></div><dl><div><dt>小計</dt><dd>{yen(estimate.subtotal)}</dd></div><div><dt>消費税</dt><dd>{yen(estimate.taxAmount)}</dd></div><div className="grand"><dt>合計</dt><dd>{yen(estimate.totalAmount)}</dd></div></dl></section>{estimate.terms&&<section className="invoice-notes"><strong>見積条件</strong><p>{estimate.terms}</p></section>}{!issuer.name&&<div className="invoice-warning no-print">設定 → 請求書設定で発行者情報を入力してください。</div>}</main></div>;}
